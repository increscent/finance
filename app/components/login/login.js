import React from 'react';

export default (props) => (
  <div className="login-view">
    <h2>&nbsp;Finance&nbsp;</h2>
    <p>A simple way to budget.</p>
    <a href="/auth/google">
      Login with Google
    </a>
  </div>
);
